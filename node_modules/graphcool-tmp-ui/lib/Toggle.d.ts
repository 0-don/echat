/// <reference types="react" />
export interface ToggleProps {
    choices: string[];
    onChange: (choice: string, i: number) => void;
    activeChoice: string;
}
declare const Toggle: ({choices, onChange, activeChoice}: ToggleProps) => JSX.Element;
export default Toggle;
