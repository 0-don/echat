query FilterUserService($slug: String!) {
  filterUserService(slug: $slug) {
    id
    status
    level
    platforms
    description
    price
    per
    images {
      id
      type
      url
    }
    user {
      id
      username
      age
      gender
      country
      lastOnline
      images {
        id
        type
        url
      }
    }
  }
}
