query GetRooms {
  getRooms {
    id
    channel
    messages {
      id
      message
      roomId
      userId
      createdAt
    }
    participants {
      id
      roomId
      userId
      user {
        id
        username
        lastOnline
        images {
          id
          type
          url
        }
      }
    }
  }
}
