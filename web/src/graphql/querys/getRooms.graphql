query GetRooms {
  getRooms {
    id
    channel
    createdAt
    participants {
      id
      roomId
      userId
      user {
        id
        username
        lastOnline
        images {
          id
          type
          url
        }
      }
    }
  }
}
